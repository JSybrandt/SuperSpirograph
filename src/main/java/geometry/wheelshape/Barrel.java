package geometry.wheelshape;

/**
 * A barrel shape for a wheel
 */
public class Barrel extends WheelGeometry{
    /**
     * Constructs a barrel with
     * @param width - the width of the barrel
     * @param height - the height of the barrel
     */
    public Barrel(float width, float height){
        super(null);
    }
}
