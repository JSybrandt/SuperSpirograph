package geometry.wheelshape;

/**
 * A circle gear shape.
 */
public class Circle  extends WheelGeometry{

    /**
     * A circle shape. PenPoints will be positioned automatically based on radius.
     * @param radius the radius of the circle
     */
    public Circle(float radius) {
        super(null);
    }
}
