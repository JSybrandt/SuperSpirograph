package geometry.wheelshape;

/**
 * A trapezoid Sized Shape
 */
public class Trapezoid extends WheelGeometry{

    /**
     * A trapzoid, defined by its base width, top width, and height.
     * @param baseWidth the width of the bottom edge
     * @param topWidth the width of the top edge
     * @param height the height of the shape
     */
    public Trapezoid(float baseWidth, float topWidth, float height){
        super(null);
    }
}
