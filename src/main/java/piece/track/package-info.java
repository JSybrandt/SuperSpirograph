/**
 * The track package defines a handful of files which describe how different track components work and fit together.
 * The TrackLayout class is the overarching controller of a single track, and is comprised of many track pieces. These
 * pieces connect together to define each sections gearing. The track layout is ultimately responsible for controlling
 * how a wheel might travel over the entire track.
 */
package piece.track;