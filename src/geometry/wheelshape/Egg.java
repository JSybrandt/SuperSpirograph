package geometry.wheelshape;

import geometry.GeometryPoint;

import java.util.List;

/**
 * An egg shaped geared wheel.
 */
public class Egg extends WheelGeometry {
    /**
     * @param scale    positive value representing the general radius of a shape
     */
    public Egg(float scale) {
        super(null);
    }
}
