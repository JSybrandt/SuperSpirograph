package geometry.wheelshape;

/**
 * An Ellipse shaped wheel
 */
public class Ellipse extends WheelGeometry{

    /**
     * Builds a Elliptical gear.
     * @param length the length of the bounding rectangle of the ellipse
     * @param width the width of the bounding rectangle of the ellipse
     */
    public Ellipse(float length, float width){
        super(null);
    }
}
